<%= @repository.name %> - <%= @repository.organization.name %>

<h2>Items</h2>
<ul>
  <% @items.each do |item| %>
    <li><%= item.title %></li>
  <% end %>
</ul>

<div class="pagination">
  <%= paginate @items %>
</div>